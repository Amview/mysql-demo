#include "mainwindow.h"
#include <mysql/jdbc.h>

void MainWindow::init_data()
{
    // QTreeWidgetItem *item = new QTreeWidgetItem(dsTree, QStringList(QString::fromStdString("localhost")), CONNECT);
    // item->setIcon(0, QIcon(":/resource/connect.svg"));
    // QTreeWidgetItem *item1 = new QTreeWidgetItem(dsTree, QStringList(QString::fromStdString("127.0.0.1")), CONNECT);
    // item1->setIcon(0, QIcon(":/resource/connect.svg"));
}
